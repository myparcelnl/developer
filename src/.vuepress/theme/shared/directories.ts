import { path } from '@vuepress/utils';

export const DIR_THEME = path.resolve(__dirname, '..');

export const DIR_NODE = path.resolve(DIR_THEME, 'node');
export const DIR_CLIENT = path.resolve(DIR_THEME, 'client');
export const DIR_SHARED = path.resolve(DIR_THEME, 'shared');
