name: 'Check pull request'

on: pull_request

jobs:
  build:
    name: 'Test site build'
    uses: myparcelnl/developer/.github/workflows/build.yml@main
    with:
      ref: ${{ github.event.client_payload.pull_request.head.ref }}
